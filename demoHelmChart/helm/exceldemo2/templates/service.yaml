apiVersion: v1
kind: Service
metadata:
  name: {{ include "exceldemo2.fullname" . }}
  labels:
    {{- include "exceldemo2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "exceldemo2.selectorLabels" . | nindent 4 }}